# ── Supabase (public client keys — safe to commit) ──────────────
NEXT_PUBLIC_SUPABASE_URL=http://supabasekong-l7gihbchirkalykcytsmifhb.84.8.140.123.sslip.io
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJpc3MiOiJzdXBhYmFzZSIsImlhdCI6MTc4ODIxMjEwMCwiZXhwIjo0OTQzODg1NzAwLCJyb2xlIjoiYW5vbiJ9.-jTeQwKWC0dDExFDVDfoZu1zLIdt-KIi0Zah5jnzYeE

# ── Sentry error reporting (public client DSN) ──────────────────
# Create the project at sentry.io, then paste your DSN here (see docs/sentry-setup.md).
# The app SDK and the mcp/katlego-debug.mjs MCP server both read this.
NEXT_PUBLIC_SENTRY_DSN=

# ── Sentry source-map upload (SERVER SECRET — never commit a real value) ──
# Used only by next.config.ts to enable the upload plugin at build/deploy time.
# Prefer `export SENTRY_AUTH_TOKEN=...` in the deploy shell over this file.
SENTRY_ORG=
SENTRY_PROJECT=
SENTRY_AUTH_TOKEN=
