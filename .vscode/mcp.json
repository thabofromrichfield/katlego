{
  "//": "Sentry MCP server for VSCode / GitHub Copilot. No secrets in this file: authenticate once with `npx @sentry/mcp-server auth login` (device flow) or export SENTRY_ACCESS_TOKEN before launching VSCode. See docs/sentry-setup.md.",
  "servers": {
    "sentry": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@sentry/mcp-server"]
    }
  }
}
